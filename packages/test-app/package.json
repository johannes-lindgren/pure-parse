{
  "name": "test-app",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "check:types": "tsc -p tsconfig.json && tsc -p tsconfig.nodenext.json && tsc -p tsconfig.node.json",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "pure-parse": "workspace:*"
  },
  "devDependencies": {
    "typescript": "5.5.4",
    "vite": "8.0.11",
    "vitest": "4.1.5"
  }
}
